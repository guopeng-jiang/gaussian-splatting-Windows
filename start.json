{
  "run": [
    {
    "method": "shell.run",
    "params": {
      "message": "start cmd.exe /k activate.bat {{path.resolve(cwd, 'gaussian-splatting', 'conda_env')}} && explorer.exe .\\gaussian-splatting"
    }
  }
  ]
}